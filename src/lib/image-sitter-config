#!/bin/bash
# shellcheck disable=SC2034

IMAGE_SITTER_HOME="${IMAGE_SITTER_HOME:-$HOME/.image-sitter}"
DIRECTORY_PULL_ALWAYS="${DIRECTORY_PULL_ALWAYS:-pull-always}"
DIRECTORY_PULL_IF_NOT_EXISTS="${DIRECTORY_PULL_IF_NOT_EXISTS:-pull-if-not-exists}"
DIRECTORY_DELETE_IF_EXISTS="${DIRECTORY_DELETE_IF_EXISTS:-delete-if-exists}"
IMAGE_SITTER_CLI="${IMAGE_SITTER_CLI:-docker}"
